# JALSG-GML219 — 既知の問題・制約

随時更新（2026-07-05 時点）

次アクションは [next-action.md](next-action.md) を参照。

---

## 解析結果への疑義事項（Inquiry）対応 → 対応済み

2026年5月分（RFS/EFS-OS/中止理由/CGA7・CCIの4件）・6月分（伊藤先生からの追加照会）とも回答済み。6月26日回答Q2-4で合意した「CR後死亡(再発前)」ラベル訂正も、7月5日にプログラム3本（Inquiry02〜04）・アウトプット・Googleドキュメント側の双方に反映済み。詳細は[`docs/JALSG-GML219_対応履歴.md`](docs/JALSG-GML219_対応履歴.md)参照。

## データベースの変数名整理 → 対応表作成で対応済み

変数名そのもののリネームではなく、`データセット項目（略号）対応表`（`TMF/20260626　回答/`配下）を作成し、略号→意味の対応と命名規則を提供する形で解決した。

## ELN2017/2022リスク分類 → 対応済み（2026-07-05）

`eln2017`（SAP記載のプライマリ分類）に加え`eln2022`（Döhner et al. Blood 2022）を追加し、Baseline表（Table1）とOS/EFS/RFS生存解析（新規Fig10、KM曲線＋層別検定）に両方反映した。実データでは`eln2017`と`eln2022`はGML219-0058の1例のみ差異（Adverse→Intermediate、ELN2022でのFLT3-ITDアレル比撤廃ルールに対応）で、他120例は一致することを確認済み。

なお実装時、`otchrabn`（その他染色体異常）・`t122p13q`（t(1;22)）がSAP本文の定義どおりに`eln2017`実装で使われていなかった点、およびRUNX1/SF3B1等の遺伝子変異は好性サブタイプと併存する場合に降格させないというELNの規定を反映するよう優先順位を調整した点は、`eln2022`側でのみ修正した（`eln2017`は既報告のため変更せず）。

## 要確認：RUNX1変数の意味（データ品質）

`eln2022`実装検証時に判明：本データセットの`runx1`変数（ラベル「RUNX1変異」）が陽性の7例中6例が、同時にt(8;21)陽性（CBF-AML、RUNX1-RUNX1T1融合）でもあった。ELNの「RUNX1変異」（点変異）はCBF-AMLとはほぼ排他的に出現するはずのマーカーであるため、この変数が実際にはRUNX1-RUNX1T1融合の確認検査（t(8;21)診断の追認）を指している可能性がある。リスク分類上は影響していない（好性サブタイプを優先するロジックのため）が、Table1等でRUNX1変異の頻度として報告する場合は要注意。DM等への確認を推奨。

## Claude連携のBoxアップロードツールが共同作業者設定フォルダで使えない

2026-07-05、更新したプログラム3本を Box `program`フォルダ（[JALSG-GML219配下](https://nmccrc.app.box.com/folder/354039003645)）へ反映しようとしたところ、`upload_file_version`が「The upload operation was blocked because the file is externally accessible.」で拒否された。原因は当該フォルダに共同作業者（`hasCollaborations: true`）が設定されていること。この安全ガードはClaude連携のBox書き込み系ツール（`upload_file`/`upload_file_version`等）に組み込まれており、共同作業者が1人でも設定されたフォルダ・ファイルへは書き込み不可（読み取り系ツールは問題なく動作する）。JALSG-GML219のBoxフォルダは試験関係者との共有が前提のため、実質的にこのツール経由でのアップロードは使えない。今回はユーザーが手動アップロードで対応した。回避方法（ローカルBox Drive同期パスの利用等）は次回までに調査予定。

## 未着手の解析候補（PIより要望あり）

- CGA7の別視点での解析（個別ドメイン別、B-score連続値、時点間変化など。関心の視点の指定待ち）
- `cr1yn`（効果判定3区分）変数を追加したデータセット版の再送（7/5回答メールでは言及せず見送り。先生の希望次第で再送）
