# 作業ログ 2026-05-04

## 実施内容

### 1. programフォルダの整理
- `program/` 直下にあったSASプログラム一式を `program/202512 pgm v1/` サブフォルダに移動
- 対象ファイル（12件）:
  - `JALSG-GML219_CSVtoSASDS.sas`
  - `JALSG-GML219_STAT_Fig1_4.4.1a.EFSf.sas`
  - `JALSG-GML219_STAT_Fig2_4.4.1b.EFSp.sas`
  - `JALSG-GML219_STAT_Fig3_4.4.5.OS.sas`
  - `JALSG-GML219_STAT_Fig4_4.4.6.RFS.sas`
  - `JALSG-GML219_STAT_Table1_4.2.PtCharacteristics.sas`
  - `JALSG-GML219_STAT_Table2_4.3.Intervention.sas`
  - `JALSG-GML219_STAT_Table3_4.4.10a_AE_Any12_3_4.sas`
  - `JALSG-GML219_STAT_Table4_4.4.10b.SAE.sas`
  - `JALSG-GML219_STAT_Table5_4.4.7.CRrate.sas`
  - `JALSG-GML219_STAT_Table6_4.x.x.EFSf_cox.sas`
  - `test20260122.sas`

### 2. GitHubへのプッシュ（1回目）
- コミット: `Move programs into 202512 pgm v1 subfolder`
- ブランチ: master → `https://github.com/nnh/jalsg-gml219.git`

### 3. 未追跡ファイルのGit管理追加
- `.vscode/settings.json` をGit管理下に追加
- `CLAUDE.md` をGit管理下に追加
- `CLAUDE_20260503.md` をGit管理下に追加
- `.gitignore` に `.DS_Store` を追加（macOSシステムファイルを除外）

### 4. GitHubへのプッシュ（2回目）
- コミット: `Add untracked files and update .gitignore`
- ブランチ: master → `https://github.com/nnh/jalsg-gml219.git`

### 5. JALSG-GML219_CSVtoSASDS.sas の改善（3件修正）

対象ファイル：`program/JALSG-GML219_CSVtoSASDS.sas`

#### 修正1：`_root` パスを Windows 用に変更
- 変更前：`%let _root = /Users/akiko/Projects/NMC/Stat/JALSG-GML219;`
- 変更後：`%let _root = C:/Users/AkikoSaito/Data/NMC/Stat/JALSG-GML219;`
- 理由：Mac 環境用パスが残っていたため Windows 環境に合わせて修正

#### 修正2：`%sasds_sdtm` のインポート先を rawdata 直下に変更
- 変更前：`"&_root./input/rawdata/20251205 fixed data/GML219_cdisc_251205_1446/&dsnm..csv"`
- 変更後：`"&_root./input/rawdata/&dsnm..csv"`
- 理由：バージョン付きサブフォルダを直接指定していたため保守性が低く、rawdata 直下の CSV を参照するよう変更

#### 修正3：CGA 変数名の PROC TRANSPOSE ミスマッチを修正
- 変更前：`cga_phase = "bl"` / `cga_phase = "c1"`
- 変更後：`cga_phase = "_bl"` / `cga_phase = "_c1"`
- 背景：SAS 実行後のログで CGA 変数 28 件の WARNING を検出。診断プログラムで調査したところ、この環境（SAS 9.4 TS1M7 DBCS3170）では `PROC TRANSPOSE` の `id` に複数変数を指定した際、アンダースコアなしで値が連結される（例：`CGA1A` + `bl` → `CGA1Abl`）ことが判明。`cga_phase` の値に先頭アンダースコアを付けることで期待する変数名（`CGA1A_bl`）を生成するよう対処

### 6. SAS プログラムの実行と結果確認

- 実行：`C:\Program Files\SASHome\SASFoundation\9.4\sas.exe` でバッチ実行
- ログ：`log/JALSG-GML219_CSVtoSASDS.log`
- 終了コード：1（ラベル文内 `%）` の ERROR が残存）
- **最終データセット `ADSLIB.GML219`：128 obs × 369 vars — 正常作成**
- **Excel 出力 `input/ads/202512 data/gml219_new.xlsx`：正常作成**

#### 残存問題（未修正）
- `program/JALSG-GML219_CSVtoSASDS.sas` L387–388 のラベル文
  ```sas
  bl_blastle = "Baseline 末梢血芽球比率（%）"
  bl_myblale = "Baseline 骨髄芽球比率（%）"
  ```
- 現象：`ERROR: 値'）'nは無効なSAS名です。`
- 原因：ダブルクォート文字列内の `%）` を SAS マクロプロセッサがマクロ呼び出しとして解釈
- 対処法：`%str(%)` でエスケープするか `%` をラベルから除去（要対応）

### 7. 修正メモ MD ファイルの作成

- 作成：`program/JALSG-GML219_CSVtoSASDS_修正メモ.md`
- 内容：修正済み 3 件・残存問題 1 件・SAS 実行結果サマリーを記載

### 8. CCI 疾患別フラグの追加（17疾患 × 2時点 = 34変数）

対象ファイル：`program/JALSG-GML219_CSVtoSASDS.sas`（L259–347 を置換）

#### 追加変数一覧

| 変数名（登録時/地固め1後） | 内容 |
|---|---|
| `cci_bl_MI` / `cci_c1_MI` | 心筋梗塞 |
| `cci_bl_CHF` / `cci_c1_CHF` | うっ血性心不全 |
| `cci_bl_PVD` / `cci_c1_PVD` | 末梢血管障害 |
| `cci_bl_CVD` / `cci_c1_CVD` | 脳血管障害 |
| `cci_bl_Dem` / `cci_c1_Dem` | 認知症 |
| `cci_bl_CLD` / `cci_c1_CLD` | 慢性肺疾患 |
| `cci_bl_Col` / `cci_c1_Col` | 膠原病 |
| `cci_bl_PU` / `cci_c1_PU` | 消化性潰瘍 |
| `cci_bl_MLiv` / `cci_c1_MLiv` | 軽度肝疾患 |
| `cci_bl_SLiv` / `cci_c1_SLiv` | 肝機能障害（中等度から高度） |
| `cci_bl_DC` / `cci_c1_DC` | 糖尿病合併症 |
| `cci_bl_Hemi` / `cci_c1_Hemi` | 片麻痺 |
| `cci_bl_SR` / `cci_c1_SR` | 腎機能障害（中等度から高度） |
| `cci_bl_Met` / `cci_c1_Met` | 固形癌転移 |
| `cci_bl_Leu` / `cci_c1_Leu` | 白血病 |
| `cci_bl_Lym` / `cci_c1_Lym` | リンパ系腫瘍 |
| `cci_bl_AIDS` / `cci_c1_AIDS` | AIDS |

値：`"Y"`（MHOCCUR="Y"）/ `"N"`（該当なしまたはデータなし）

#### 実装方法
- `proc sql` で `tmp2d_cci_fl` に 36 フラグ変数（既存の `cci_bl_fl`/`cci_c1_fl` 含む）を一括生成
- `data tmp2d_cci` で `array _fl{*} $` を使ったループにより欠損 `""` を `"N"` に補完
- 変数数：372 → 406 の見込み（SAS実行後に確認要）
