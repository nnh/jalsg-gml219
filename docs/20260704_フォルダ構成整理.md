# JALSG-GML219 フォルダ構成の混乱の整理 (2026-07-04)

**2026-07-04中に本人確認のうえ、旧フォルダ（B）は削除済み。** 経緯は末尾の「対応結果」を参照。
以下の本文は削除前の調査時点の記録として残す。
2025年12月・2026年5月31日にPIへ送付した解析結果のプログラム・アウトプットは、削除前に内容を確認し、CURに現存することを確認済み。

## 登場する4つの場所

| # | 場所 | 種類 | 状態 |
|---|---|---|---|
| A | `C:\Users\AkikoSaito\Data\NMC\Stat\JALSG-GML219` | ローカル（現在の作業フォルダ） | Gitリポジトリ。GitHubの③と連携 |
| B | `C:\Users\AkikoSaito\Data\JALSG-GML219`（`NMC\Stat`なし） | ローカル（旧フォルダ） | Git初期化のみでコミット0件、どこにもpushされていない |
| C | GitHub `https://github.com/nnh/jalsg-gml219` | リモートGit | Aのprogram配下のみ追跡（データ・出力は.gitignoreで除外） |
| D | Box `https://nmccrc.app.box.com/folder/336449975048`（JALSG-GML219）とその配下の`program`サブフォルダ`354039003645` | クラウドストレージ | ①と②は同じ場所（②は①の中の`program`フォルダ）。混乱の原因ではなく親子関係だった |

**結論: Boxの2つのURLは別々の場所ではなく、`.../JALSG-GML219`フォルダとその中の`program`サブフォルダなので、Box自体は1箇所。混乱の主因はローカルにAとBの2つの作業フォルダが存在していること。**

## A（現行フォルダ）とB（旧フォルダ）の関係

途中でローカルの作業場所が `Data\JALSG-GML219` → `Data\NMC\Stat\JALSG-GML219` に引っ越しされたが、移行が一部しか行われていない。

### Bにしかないもの（Aには存在しない）

- ~~`TMF\JALSG-GML219 aCRF\`・PRT・SAP がAに無い~~ → **訂正: 実際はAにも同一ファイル名で存在していた（当初の調査ミス）。移動不要と判断。**
- **`save\`フォルダ（約73MB、sas7bdatの中間データセット群）** — Box側の`save`フォルダは空、Aの`save`も空。**このデータはB以外のどこにも存在しない。** → 本人確認の結果、ETLプログラム(`CSVtoSASDS.sas`)で再生成可能なため**移動不要と判断**。
- **`program\202512 pgm v1\`と`output\202512 rtf\`、`log\202512log\`の"真のオリジナル"**（2025年12月9日付のタイムスタンプ）。Aにも同名フォルダがあるが、Aの方はファイルの更新日時が2026年5月4-5日になっており、Bからコピーされた際に触られた形跡がある（中身はおそらく同一だが要確認）。
- **`program\scratch_inquiry_202605\`、`output\old_inquiry_202605\`、`log\old_inquiry_202605\`** — 2026年5月照会対応の下書き版（`check_inquiry_202605.sas`等）。ファイル名の"old"「scratch」から、最終版（Aにある`JALSG-GML219_Inquiry01〜04*.sas`)ができた後の使用済み下書きと判断される。
- `input\ads\`, `input\rawdata\`, `input\sdtm\`, `input\ext\` の一部（rawdataはA:21件 / B:22件で1件差があり、完全一致ではない）。

### Aにしかないもの（Bには存在しない）

- **`TMF\20260626　回答\`**（伊藤先生ご照会への回答、変数名対応表 `データセット項目（略号）対応表_20260626.xlsx`）— 2026年6月26日の新しい照会対応で、Bにはまだ存在しない。issues.mdに書いた「データベース変数名を研究者にわかりやすくしたい」という相談は、実はこの対応表としてすでに着手されている。
- **`TMF\Gmail - 東京医大・伊藤よりお尋ね.pdf`**
- **`TMF\Inquiry_202605_response\`**（4項目照会回答一式、README/summary含む）— Bには対応する最終版はなく、下書き（scratch/old）のみ。
- Gitで管理されているCLAUDE.md・overview.md・issues.md・next-action.md・.claude/settings.json等（2026-07-04の初期設定で追加）。
- ルート直下および`program\`直下に散らばった2026-05-05解析の中間ファイル（`Fig*.png`, `*.log`, `*.lst`）— `output\`や`program\`内に整理済みのコピーと重複しており、雑然としている（後述）。

## Boxの状態（③）

BoxのTMFには、Aには無いaCRF/PRT/SAPが存在する一方、Aにしかない`20260626　回答`フォルダやGmail PDFはBoxにまだアップロードされていない。**BoxもAとBのどちらとも完全一致しない、3つ目の中間状態。**

- `save\`（Box）: 空。Bのローカルにしかない約73MBの中間データセットはBoxにも上がっていない。
- `program\`（Box）: ほぼA/Bの2025年12月・2026年5月分と一致（Inquiry01〜04含む、2026-05-31更新）。
- `20260525 PGM`（Box、空フォルダ）: 中身なし。放置されたプレースホルダとみられる。

## PIへの送付実績（削除禁止対象・現状の所在）

| 送付時期 | 内容 | 現在の所在 |
|---|---|---|
| 2025年12月9日 | Table1-5, Fig1-4（12月版解析） | A: `program\202512 pgm v1\`, `output\202512 rtf\`, `log\202512log\`（2026-05にコピーされた版） / B: 同名フォルダ（2025-12-09オリジナルのタイムスタンプ） / Box: `program\202512 pgm v1\` |
| 2026年5月31日 | Inquiry01〜04（照会対応4項目） | A: `program\JALSG-GML219_Inquiry01〜04*.sas`（Git管理下）, `output\JALSG-GML219_Inquiry01〜04*.rtf`, `TMF\Inquiry_202605_response\`（回答MD一式） / Box: `program\`配下に同名ファイルあり（2026-05-31更新） |

いずれも**削除していない**。AとBの両方に現存する。

## 気になる点（対応不要・記録のみ）

1. **Aのルート直下・`program\`直下に散らばった2026-05-05分の`.png`/`.log`/`.lst`** — `output\`と`log\`に整理済みの正式コピーがあるはずで、重複・散らかりの原因になっている。削除するなら整理済みコピーとの同一性を確認してから。
2. **Bはgit初期化されているが0コミット** — pushもされておらず、GitHub上には存在しない。Aと同じ内容をコミットしようとすると衝突する可能性があるため、Bを今後Git管理する場合は要注意。
3. **`save\`の中間データセット（73MB）はBにしか存在しない** — Aにもコピーされておらず、Boxにも上がっていない。唯一のコピーなので、消失リスクという観点では最優先で確認したい対象。
4. **TMFコア文書（aCRF/PRT/SAP）がAのローカルに無い** — Box or Bから取得すれば復元可能。

## 対応結果 (2026-07-04)

本人確認のうえ、以下の対応を実施した。

1. Bにしかなかった以下2件をAにコピー
   - `input\rawdata\saihi.csv`（rawdataの1件差分）
   - `program\202512 pgm v1\SurvivalPlot1,3,5,7,9.png`（2025年12月送付分の図表、Aの同名フォルダに欠けていた）
2. `save\`（73MB、ETLで再生成可能と確認）、`program\scratch_inquiry_202605\`等の下書き（削除しても実害なしと確認）、その他Aと重複する内容は移動せず
3. 旧フォルダ B（`C:\Users\AkikoSaito\Data\JALSG-GML219`、Git初期化のみでコミット0件）を削除

削除前に確認した差分の詳細:
- `TMF\`: AとBで完全一致（当初「Aに無い」としたのは調査ミス）
- `program\202512 pgm v1\`: `JALSG-APL219R_STAT_Table6_5.4.8.EFSf_cox.sas`（B）とA既存の`JALSG-GML219_STAT_Table6_4.x.x.EFSf_cox.sas`は同一内容のリネーム違い→移動不要。`CSVtoSASDS.sas`はA/Bで文字コードが異なる（Aは修正済みCP932、Bは旧UTF-8版）のみで、Aの方が正。
- `output\202512 rtf\`, `log\202512log\`: A/Bで完全一致
- `input\ads\`: B側の`.bak`ファイル数点のみの差分で、実質的な情報差なし

## 今回行ったこと / 行っていないこと（当初の調査時点）

- 行った: A・B・Box・GitHubの中身を読み取り専用で確認し、本ファイルに整理。その後、上記「対応結果」のとおり必要なファイルをコピーしBを削除。
- Google Docs「伊藤先生からのお問合せ対応」(2026-05-31)とBoxフォルダ`385797988500`(JALSG-GML219_20260531 PIへ提出)が、2026年5月照会対応の実際の最終送付物であることを確認。
- SASプログラムの文字化け（AE24ラベル「斑状丘疹状?疹」→「斑状丘疹状皮疹」）を`JALSG-GML219_CSVtoSASDS.sas`・`JALSG-GML219_STAT_Table5_4.4.10a.AE.sas`で修正済み。
